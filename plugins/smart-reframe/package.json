{
  "name": "@colorwave/plugin-smart-reframe",
  "version": "0.1.0",
  "description": "Subject-aware aspect-ratio reframe. Solves a smooth pan/zoom path that keeps the main subject in frame.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --target=es2022 --outfile=dist/smart-reframe.js --legal-comments=none --external:@xenova/transformers --external:@mediapipe/tasks-vision",
    "size": "wc -c dist/smart-reframe.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
