{
  "name": "@colorwave/plugin-beat-sync",
  "version": "0.1.0",
  "description": "Beat-detect a track, snap clip cuts to beats, flash transitions on hits.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --target=es2022 --outfile=dist/beat-sync.js --legal-comments=none",
    "size": "wc -c dist/beat-sync.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
