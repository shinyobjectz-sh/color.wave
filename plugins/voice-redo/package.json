{
  "name": "@colorwave/plugin-voice-redo",
  "version": "0.1.0",
  "description": "Edit a VO transcript, ElevenLabs regenerates audio at the same duration. The 'AI Resolve' workflow.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --target=es2022 --outfile=dist/voice-redo.js --legal-comments=none",
    "size": "wc -c dist/voice-redo.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
