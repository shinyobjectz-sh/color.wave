{
  "name": "@colorwave/plugin-bg-remove",
  "version": "0.1.0",
  "description": "One-click background removal for images and video clips. RMBG-2 + SAM2 via HF Inference, fal.ai fallback.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --target=es2022 --outfile=dist/bg-remove.js --legal-comments=none",
    "size": "wc -c dist/bg-remove.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
