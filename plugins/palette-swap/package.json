{
  "name": "@colorwave/plugin-palette-swap",
  "version": "0.1.0",
  "description": "Recolor the entire composition with a one-click palette preset",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --target=es2022 --outfile=dist/palette-swap.js --legal-comments=none"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
